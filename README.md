A RoomBooking application that currently has one endpoint: `booking/add`. This endpoint checks if there are any existing bookings.

This application will uses a CI/CD pipeline to automate testing. At the moment, the testing is integration testing that uses a Testcontainer for PostgreSQL.
