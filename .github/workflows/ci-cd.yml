# .github/workflows/ci-cd.yml
name: ci-cd


on:
  push:
    branches:
      - main

  # pull_request:
  #   branches:
  #     - main

#comment just to see if this work 10

jobs:
  CI:
    uses: ./.github/workflows/ci.yml
  CD:
    needs: CI
    uses: ./.github/workflows/cd.yml