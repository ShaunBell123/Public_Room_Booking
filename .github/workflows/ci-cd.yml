# .github/workflows/ci-cd.yml
name: ci-cd

permissions:
  contents: read
  packages: write


on:
  push:
    branches:
      - main

  # pull_request:
  #   branches:
  #     - main

jobs:
  CI:
    uses: ./.github/workflows/ci.yml
  CD:
    needs: CI
    uses: ./.github/workflows/cd.yml