# .github/workflows/ci-cd.yml
name: ci-cd


on:
  push:
    branches:
      - main

  # pull_request:
  #   branches:
  #     - main

#comment just to see if this work 5

jobs:
  format:
    uses: ./.github/workflows/ci-format.yml

  test:
    needs: format
    uses: ./.github/workflows/ci-tests.yml